<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dongduk.cs.moaread.dao.mybatis.mapper.BookMapper">
    <insert id="insertBook" parameterType="Book">
        INSERT INTO BOOK
        (ISBN, TITLE, AUTHOR, PUBLISHER, PUBLISH_DATE, DESCRIPTION, PRICE, IMAGE)
        VALUES
            (#{isbn}, #{title}, #{author}, #{publisher}, #{publishDate}, #{description}, #{price}, #{image})
    </insert>

    <select id="findByIsbn" resultType="Book">
        SELECT *
        FROM BOOK
        WHERE ISBN = #{isbn}
    </select>
</mapper>
